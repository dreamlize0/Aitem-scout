# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Node (루트에서 임의 도구가 install되더라도 커밋 안 되도록 보호)
node_modules/

# 로컬 env (서브 디렉터리 .gitignore와 중복 안전망)
.env
.env.local
.env.*.local

# 로그/임시
*.log
.tmp/
